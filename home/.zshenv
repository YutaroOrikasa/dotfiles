# For ubuntu /etc/zsh/zshrc.
# Avoid calling compinit in /etc/zsh/zshrc.
# compinit should be called once at end of ~/zshrc.
skip_global_compinit=1

# for apple /etc/zshrc_Apple_Terminal
SHELL_SESSIONS_DISABLE=1
